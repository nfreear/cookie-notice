# https://travis-ci.org/nfreear/cookie-notice
# https://docs.travis-ci.com/user/languages/javascript-with-nodejs/

language: node_js

node_js: 8

git:
  depth: 8

cache:
  directories:
  - node_modules # NPM packages.

# install: npm install

# script: npm test

after_script:
- pgrep -lf '(live-server|node|phantom)'
- ls -al dist/
# Accessibility issues won't fail the build, for now!
- npm run pa11y


# End.
